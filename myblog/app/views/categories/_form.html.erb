<%= form_for @category do |f| %>
<p>
  <%= f.text_field :category, placeholder: 'category name' %>
  <% if @category.errors.messages[:category].any? %>
  <span class="error"><%= @category.errors.messages[:category][0] %></span>
  <% end %>
</p>
<%= f.submit %>
<% end %>
